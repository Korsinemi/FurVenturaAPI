{
    "version": 2,
    "builds": [
        {
            "src": "./src/index.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "./src/index.js",
            "methods": [ "GET", "PUT", "PATCH", "POST", "DELETE", "OPTIONS" ],
            "headers": {
                "Access-Control-Allow-Origin": "*"
            }
        }
    ]
}